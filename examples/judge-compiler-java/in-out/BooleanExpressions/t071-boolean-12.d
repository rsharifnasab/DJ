int main() {
    int a;
    int b;

    bool r;

    a = ReadInteger();
    b = ReadInteger();
    r = a < b;
    Print(r);

    a = ReadInteger();
    b = ReadInteger();
    r = a < b;
    Print(r);

    a = ReadInteger();
    b = ReadInteger();
    r = a < b;
    Print(r);

    a = ReadInteger();
    b = ReadInteger();
    r = a < b;

    Print(r);
}
